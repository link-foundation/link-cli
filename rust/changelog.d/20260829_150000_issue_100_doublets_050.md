---
bump: minor
---

Updated the `doublets` basis crate to 0.5.0 (both the `link-cli` and `clink-wasm` lockfiles), which brings the upstream `doublets::decorators` layer into reach for the Rust storage stack.
